## What does this PR do?

A clear description of the change and why it's needed.

## Related issue

Closes #

## Checklist

- [ ] Ran `./gradlew lintRelease` locally
- [ ] Tested on a real device (not just emulator)
- [ ] Kept the change focused — one logical change per PR
- [ ] Updated `CHANGELOG.md` under `## [Unreleased]` if this is user-facing (new feature, fix, behavior change)
- [ ] If touching `QueueRepository.kt`: `setLinks()` still **merges** into the existing queue instead of replacing it

## Screenshots / recordings

If this changes UI, add a before/after screenshot or screen recording.

## Additional context

Anything else worth knowing (edge cases considered, follow-up work planned, etc.).
